name: view_model_test
description: A new Flutter project.

publish_to: 'none' 


version: 1.0.0+1

environment:
  sdk: ">=2.17.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  mockito: 5.2.0
  mocktail: 0.3.0
  
  view_model:
    path: ../view_model

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: 2.1.7

flutter:
  uses-material-design: true